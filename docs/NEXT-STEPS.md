# NEXT STEPS — очередь задач

По приоритету — сверху то, что имеет смысл сделать раньше остального.

## ⚠️ Срочно: у P1 и P4 есть срок годности (проверено 2 августа 2026)

- **P4 (генерация изображения, `gemini-2.5-flash-image`)** — сейчас работает
  и бесплатно (до 500 изображений/день), но у модели запланировано
  отключение **2 октября 2026**. Официальная замена от Google
  (`gemini-3.1-flash-image-preview`, «Nano Banana 2») **не имеет бесплатного
  тарифа API вообще** — только платный, ориентировочно $0.045–0.067 за
  изображение. Решение пока отложено (вернуться к вопросу позже) — без
  действий до октября приложение перестанет бесплатно генерировать картинки.
- **P1 (разворачивание промта, `gemini-2.5-flash`)** — тоже запланировано к
  отключению, **16 октября 2026**. Здесь проще: замена `gemini-3-flash-preview`
  тоже бесплатна в Gemini API — просто сменить модель в коде
  (`GeminiPromptExpansionService.kt`) до этой даты.
- Обе даты — "не раньше этой даты" по формулировке Google, могут сдвинуться,
  но полагаться на сдвиг не стоит.

## 1. Обновить screens-and-procedures.pdf/html под текущий вид v1
Сейчас PDF (и исходный HTML) описывает макеты и процедуры v0 — включая
экраны S3/S4/S5, оплату, разделы бэкенда, и старую компоновку S1 без плашки
`S1.T0`. Нужно перегенерировать под актуальный `SPEC.md`.

## 2. Придумать название приложения
Плейсхолдер "MyApp" в плашке `S1.T0` (`app_banner_title` в `strings.xml`) —
заменить одной строкой, когда появится реальное название.

## 3. Решить, что в нижней части S1
Место под блоком "Я правильно поняла?" сейчас намеренно пустое (было место
кнопки входа/плашки регистрации в v0) — решить, нужно ли туда что-то, или
оставить как есть.

## 4. Уточнить дневной лимит бесплатного тарифа Gemini Flash
Источники расходятся (ориентировочно 50–500 запросов/день) — проверить в
консоли Google AI Studio при получении/использовании ключа.

---

Уже сделано (см. `START-HERE.md` для полного статуса):
- Разворачивание промта через ИИ (`P1`) — Gemini API, текст + фото.
- Прикрепление фото (`S1.B1`) — системный Photo Picker.
- Генерация изображения (`P4`) — одна модель, Gemini Flash (бесплатно).
- Сохранение результата (`S2.B1`, `P12`) — `ImageSaver.kt`, сохранение в
  галерею через MediaStore (с runtime-разрешением на API 26-28, без него на 29+).
- Убраны регистрация, оплата (Google Play Billing + ЮKassa), уровни доступа,
  водяной знак и экран профиля — весь код удалён, `build.gradle.kts` и
  `AndroidManifest.xml` тоже почищены. Полная версия с этим функционалом
  сохранена отдельно в `ContentApp_v0.zip`.
- Пересмотрен дизайн S1/S2: добавлена плашка названия приложения (`S1.T0`,
  общий компонент `AppNameBanner.kt` для обоих экранов), `S1.F1` растянут на
  всю ширину, `S1.B1`/`S1.B2` перенесены под него и уравнены по размеру с
  `S1.B3`/`S1.B4` (44dp, иконки 24dp), `S1.F2` увеличен вдвое по высоте,
  `S1.C1` сдвинут ниже. `S2.B2` подтверждён как не сбрасывающий поля `S1`.

Каждый пункт можно взять в чат прямо по ID из `SPEC.md` — например,
"давай сделаем пункт 1, обновим PDF".
