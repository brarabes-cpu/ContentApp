# ContentApp — техническая документация

Android-приложение (Kotlin + Jetpack Compose): пользователь вводит короткую
фразу-желание, приложение через ИИ разворачивает её в детализированный промт,
показывает подтверждение и генерирует изображение.

**v1** — без регистрации, оплаты и уровней доступа. Версия документа
соответствует состоянию проекта после удаления этого функционала (см.
`docs/DECISIONS.md`, раздел "v1 — упрощение"). Полная версия с монетизацией
сохранена отдельно в `ContentApp_v0.zip`.

---

## 1. Обзор

Пользователь вводит короткую фразу ("собачка на траве"), приложение
разворачивает её в детализированный промт через GigaChat API (Сбер), показывает
подтверждение и генерирует изображение через встроенную функцию text2image того
же GigaChat API. Один провайдер на обе задачи, работает из РФ без VPN и без
зарубежной карты (см. `docs/DECISIONS.md` — изначально использовался Gemini, но
он заблокирован для российских IP на уровне API; для P4 также пробовались
Kandinsky/FusionBrain и YandexART, но не подошли по надёжности/требованию карты).

---

## 2. Структура проекта

```
ContentApp/
├── settings.gradle.kts, build.gradle.kts       — конфигурация Gradle-проекта
├── README.md                                    — краткая памятка по проекту
├── app/
│   ├── build.gradle.kts                         — зависимости модуля (Compose, OkHttp)
│   └── src/main/
│       ├── AndroidManifest.xml                  — манифест
│       ├── java/com/example/contentapp/
│       │   ├── MainActivity.kt                  — точка входа
│       │   ├── navigation/
│       │   │   └── AppNavigation.kt             — граф экранов (main -> result), состояние приложения
│       │   ├── ai/
│       │   │   ├── PromptExpansionService.kt          — интерфейс P1
│       │   │   ├── GigaChatPromptExpansionService.kt   — реализация через GigaChat API (Сбер)
│       │   │   └── PromptExpansionResult.kt            — Success/Failed
│       │   ├── content/
│       │   │   ├── ImageGenerationService.kt              — интерфейс P4
│       │   │   ├── GigaChatImageGenerationService.kt        — реализация через text2image GigaChat API
│       │   │   ├── ImageGenerationResult.kt                — Success/Failed
│       │   │   └── ImageSaver.kt                            — сохранение в галерею (MediaStore)
│       │   └── ui/
│       │       ├── theme/                       — Color.kt, Type.kt, Theme.kt
│       │       └── screens/
│       │           ├── AppNameBanner.kt         — плашка названия приложения (S1.T0), общая для S1/S2
│       │           ├── MainScreen.kt            — ввод промта, вложение фото (S1)
│       │           ├── LoadingOverlay.kt        — оверлей ожидания (O1)
│       │           └── ResultScreen.kt          — экран результата (S2)
│       └── res/values/
│           ├── strings.xml                      — все тексты интерфейса
│           └── themes.xml                        — базовая Material-тема
└── design/
    ├── *.svg                                     — вайрфреймы экранов v0 (этап проектирования)
    └── *.mermaid                                 — схемы v0 (модель данных и роутинга API)
```

---

## 3. Внутренняя архитектура приложения

### 3.1. Единый источник состояния

`AppNavigation` — корневой компонент, держит состояние текущего запроса
(`lastPrompt`, `resultImageBytes`, `contentErrorMessage`, `isLoading`,
`saveMessage`/`saveIsError`) и создаёт единственные экземпляры сервисов
(`PromptExpansionService`, `ImageGenerationService`). Экраны (`MainScreen`,
`ResultScreen`) получают готовые колбэки и не содержат бизнес-логики.

### 3.2. Граф навигации

| Маршрут | Экран | Переходы |
|---|---|---|
| `main` | `MainScreen` | → `result` (по одной из двух кнопок фиксации промта) |
| `result` | `ResultScreen` | → назад на `main` ("Новый запрос") |

### 3.3. Разворачивание промта (P1) и генерация изображения (P4)

Два разных российских провайдера (Сбер), с разной авторизацией — намеренно не
унифицированы под одну абстракцию (см. `docs/DECISIONS.md`).

- **P1** — `GigaChatPromptExpansionService` вызывает GigaChat API (OAuth2,
  `GIGACHAT_AUTH_KEY` из `local.properties`, access-токен на 30 минут кешируется
  и обновляется автоматически) с текстом (и фото, если приложено — через отдельный
  эндпоинт `/files`), возвращает развёрнутый промт для блока "Я правильно
  поняла?" (`S1.C1`). Требует доверенный сертификат НУЦ Минцифры
  (`res/xml/network_security_config.xml`).
- **P4** — `GigaChatImageGenerationService` вызывает тот же `/chat/completions`,
  что и P1, но с `"function_call": "auto"` и промтом со словом "нарисуй" — модель
  сама вызывает встроенную функцию `text2image`. Ответ содержит `<img src="file_id">`
  внутри текста, откуда парсится id, дальше отдельным запросом
  `/files/{file_id}/content` скачивается сама картинка.

---

## 4. Реализованные экраны

### 4.1. MainScreen (S1)

Сверху — плашка с названием приложения (`S1.T0`, компонент `AppNameBanner.kt`,
во всю ширину, временный текст "MyApp"). Ниже — подпись "Что хочу" над полем
ввода промта (`S1.F1`, во всю ширину), под ним в ряд кнопка-скрепка
(`S1.B1`, приложить фото) и кнопка Enter (`S1.B2`) — обе делят ширину поровну,
единый размер со всеми остальными кнопками экрана (44dp). После Enter
появляется блок "Я правильно поняла?" (`S1.C1`) с редактируемым текстом
(`S1.F2`, увеличенной высоты) и двумя кнопками фиксации ("Сделать как я хочу"
/ "Сделать как ты поняла"). Ниже блока — пустое пространство (открытый вопрос,
см. `NEXT-STEPS.md`).

### 4.2. ResultScreen (S2)

Та же плашка `S1.T0` сверху (переиспользуется `AppNameBanner.kt`). Дальше —
"карточка в контексте": промт отображается полностью, ниже — результат
генерации (или сообщение об ошибке), кнопки "Сохранить" и "Новый запрос".

### 4.3. LoadingOverlay (O1)

Оверлей ожидания результата поверх текущего экрана (не отдельный переход) —
используется и на P1 (внутри `MainScreen`), и на P4 (поверх `ResultScreen`).

---

## 5. Что реализовано и что осталось

### Реализовано
- Полный UI-каркас двух экранов на Jetpack Compose (S1, S2).
- Разворачивание промта через GigaChat API (P1), с учётом приложенного фото.
- Генерация изображения через встроенную функцию text2image GigaChat API (P4).
- Сохранение результата в галерею через `MediaStore` (`ImageSaver.kt`).
- Единая цветовая палитра и типографика (`ui/theme/`).

### Открытые вопросы (см. `docs/NEXT-STEPS.md`)
- Сертификаты НУЦ Минцифры нужно скачать и положить в проект отдельно —
  не входят в архив.
- Точный размер бесплатного тарифа GigaChat по факту использования (единый лимит
  на P1 и P4, т.к. это один и тот же аккаунт) — уточнить в личном кабинете.
- Дальнейшая доработка экранов и API — по ходу обсуждения в чате.

---

## 6. Как открыть проект

1. Распаковать архив.
2. Android Studio → **File → Open** → выбрать папку `ContentApp` (где лежит
   `settings.gradle.kts`).
3. Скопировать `local.properties.example` в `local.properties`, вписать
   `GIGACHAT_AUTH_KEY` (единственный ключ, используется и для P1, и для P4).
4. Скачать сертификаты НУЦ Минцифры в `app/src/main/res/raw/` (см.
   `docs/NEXT-STEPS.md`, иначе сборка упадёт).
5. Дождаться Gradle sync (нужен интернет для загрузки зависимостей).
6. Запустить на эмуляторе/устройстве.

Требования: Android Studio Koala или новее, JDK 17.

---

## 7. Материалы этапа проектирования (`design/`)

Вайрфреймы и схемы из `design/` относятся к v0 (включают экраны входа/оплаты,
которых в v1 больше нет) — оставлены как исторический материал этапа
проектирования, не отражают текущий код:

- `main-screen.svg`, `login-screen.svg`, `loading-overlay.svg`,
  `result-screen-registered.svg`, `result-screen-unregistered.svg`,
  `main-screen-attach-icon.svg` — вайрфреймы экранов.
- `data-model.mermaid` — модель данных.
- `api-routing-flow.mermaid` — логика роутинга запросов для
  авторизованного/гостя.
